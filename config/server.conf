#!/usr/bin/env bash
# arch-bootstrap role layer — server / headless
# Counterpart of desktop.conf: hosts opt in near the top of their preset:
#   source "${BASH_SOURCE[0]%/*}/server.conf"
# Currently adds nothing — default.conf is already server-safe (no graphical
# packages). Server-only additions belong here instead of being duplicated
# across host presets.
